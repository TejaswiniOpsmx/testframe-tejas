include "api-automation", "pipeline-builder", "examples", "common"
rootProject.name = 'automation'

def setBuildFile(project) {
    project.buildFileName = "${project.name}.gradle"
    project.children.each {
        setBuildFile(it)
    }
}

rootProject.children.each {
    setBuildFile it
}
